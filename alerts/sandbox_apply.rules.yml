
groups:
- name: sandbox_apply
  rules:
  - alert: SandboxApplyFailed
    expr: sandbox_apply_success{ats="greenhouse"} != 1
    for: 30m
    labels:
      severity: critical
    annotations:
      summary: "sandbox_apply_success ≠ 1 for >30m"
